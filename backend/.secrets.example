# =============================================================================
# MAGI — API 密钥文件
# =============================================================================
# 复制为 .secrets 并填入真实密钥（已加入 .gitignore，不会提交）：
#   cp .secrets.example .secrets
#
# 只需设置当前 AI_PROVIDER 对应的密钥即可。
# =============================================================================

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
SILICONFLOW_API_KEY=
DEEPSEEK_API_KEY=
